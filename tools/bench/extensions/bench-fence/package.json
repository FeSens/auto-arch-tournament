{
  "name": "@auto-arch-tournament/bench-fence",
  "version": "0.1.0",
  "description": "pi-coding-agent extension: enforce read/write/bash path allowlists for benchmark runs.",
  "main": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0"
  }
}
