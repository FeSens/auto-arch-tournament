# NeurIPS pilot run — Opus 4.7 only.
#
# Prior pilot batches preserved in bench/pilot/results.jsonl:
#   - gpt-5.5 (3 reps, +51%/+51%/+29%)
# Skipped via load_done_set on retry; this YAML adds only opus.
#
# Routed via opencode → OpenRouter
# (auth: OPENROUTER_API_KEY in ~/.bench-keys.env, picked up by opencode).
# xhigh effort.

models:
  - name: opus-4_7
    provider: opencode
    model: openrouter/anthropic/claude-opus-4.7
    oauth: true
