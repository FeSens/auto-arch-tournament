{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["id","title","category","motivation","hypothesis","expected_impact","changes"],
  "properties": {
    "id":       {"type": "string", "pattern": "^hyp-[0-9]{8}-[0-9]{3}(-r[0-9]+s[0-9]+)?$"},
    "title":    {"type": "string", "minLength": 5},
    "category": {"type": "string", "enum": ["micro_opt","structural","extension","predictor","memory"]},
    "motivation":  {"type": "string", "minLength": 20},
    "hypothesis":  {"type": "string", "minLength": 20},
    "expected_impact": {
      "type": "object",
      "required": ["fitness_delta_pct","confidence"],
      "properties": {
        "fitness_delta_pct": {"type": "integer"},
        "confidence": {"type": "string", "enum": ["low","medium","high"]}
      }
    },
    "changes": {
      "type": "array", "minItems": 1,
      "items": {
        "type": "object",
        "required": ["file","description"],
        "properties": {
          "file":        {
            "type": "string",
            "description": "Must be under rtl/ (any extension) or be a test/test_*.py cocotb suite. Anything else is rejected by the runtime sandbox check.",
            "pattern": "^(rtl/[^.].*|test/test_[^/]+\\.py)$"
          },
          "description": {"type": "string"}
        }
      }
    }
  }
}
