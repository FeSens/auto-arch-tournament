# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/

# Toolchain (downloaded by setup.sh)
.toolchain/

# Vendored as submodule (initialized in Phase 4)
formal/riscv-formal/

# Formal run output (regenerated per run; surfaced via stdout to the orchestrator)
formal/last_run.log

# Build artifacts
out/
generated/
sim_build/
test/cosim/obj_dir/
test/cosim/obj_dir_vex/
test/cosim/sim_build/
test/**/sim_build/
test/*.result.xml
test/results.xml
*.vcd
*.fst
*.gtkw

# Bench ELFs (rebuild from sources)
bench/programs/*.elf
bench/programs/*.o
bench/programs/*.bin
bench/programs/*.hex

# Experiment artifacts (each iteration appends to log.jsonl; per-iter worktrees)
experiments/worktrees/
experiments/progress.png
experiments/log.jsonl
experiments/hypotheses/*.yml
experiments/hypotheses/*.yaml

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code workspace
.claude/
